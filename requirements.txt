# ============================================================
# Dependências do BOT (é o que a imagem Docker instala)
# Para o dashboard/microfone/receptor, use requirements-dashboard.txt
# ============================================================

# Núcleo do bot Discord
discord.py>=2.3.0
python-dotenv>=1.0.0
PyNaCl>=1.5.0

# Captura de áudio do Discord (versão alpha mais recente)
discord-ext-voice-recv>=0.5.0a167

# Integração com IA (Google Gemini)
google-generativeai>=0.3.0

# Áudio: download e TTS
yt-dlp>=2024.1.1
edge-tts>=6.1.0

# Banco de dados
aiosqlite>=0.19.0

# API HTTP de controle
aiohttp>=3.9.0

# Health checks (/status, /uptime)
psutil>=5.9.0

# Fallback de mixagem PCM quando audioop não existe (Python 3.13+)
numpy>=1.24.0
